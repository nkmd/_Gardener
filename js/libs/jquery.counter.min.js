(function($){"use strict";$.fn.countTo=function(options){options=$.extend({},$.fn.countTo.defaults,options||{});var loops=Math.ceil(options.speed/options.refreshInterval),increment=(options.to-options.from)/loops;return $(this).each(function(){var _this=this,loopCount=0,value=options.from,interval=setInterval(updateTimer,options.refreshInterval);function updateTimer(){value+=increment;loopCount++;$(_this).html(value.toFixed(options.decimals));if(typeof options.onUpdate==="function"){options.onUpdate.call(_this,value)}if(loopCount>=loops){clearInterval(interval);value=options.to;if(typeof options.onComplete==="function"){options.onComplete.call(_this,value)}}}})};$.fn.countTo.defaults={from:0,to:100,speed:1e3,refreshInterval:100,decimals:0,onUpdate:null,onComplete:null};if($(".ale_counter .counter.fromzero").length){$(".ale_counter .counter.fromzero").each(function(){if(!$(this).hasClass("finished")){$(this).addClass("finished");$(this).appear(function(){$(this).css("visibility","visible");var $max=parseFloat($(this).text());$(this).countTo({from:0,to:$max,speed:1500,refreshInterval:100})},{accX:0,accY:-200})}})}})(jQuery);